import { Box } from "@mui/material";
import { NextPage } from "next";
import React from "react";

const Top: NextPage = () => {
  return (
    <Box
      sx={{
        display: "flex",
        flexDirection: "row",
        backgroundColor: "#fffee6",
      }}
    ></Box>
  );
};

export default Top;
